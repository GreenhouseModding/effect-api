{
    "schemaVersion": 1,
    "id": "${mod_id}-base",
    "version": "${version}",
    "name": "${mod_name}",
    "description": "${mod_description}",
    "authors": [
        "${mod_author}"
    ],
    "contributors": [
        ${fabric_mod_contributors}
    ],
    "contact": {
        "homepage": "${homepage}",
        "issues": "${issues}",
        "sources": "${sources}"
    },
    "license": "${mod_license}",
    "icon": "${mod_id}.png",
    "environment": "*",
    "entrypoints": {
        "preLaunch": [
          "dev.greenhouseteam.effectapi.impl.EffectAPIBaseFabricPre"
        ]
    },
    "mixins": [
        "${mod_id}-base.mixins.json"
    ],
    "accessWidener": "${mod_id}-base.accesswidener",
    "depends": {
        "fabricloader": "${fabric_loader_range}",
        "fabric-api": "*",
        "minecraft": "${fabric_minecraft_version_range}",
        "java": ">=${java_version}",
        "${mod_id}-base": ">=${version}"
    }
}